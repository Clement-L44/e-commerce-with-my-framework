# HTTP request processing rule
# Ex : With URL Rewriting -> /article/1 = /page=article&id=1
# RewriteEngine On : Indicates that we are using the url rewrite engine
# FallbackResource : If we can't find the requested file on the url, we redirect to index.php

RewriteEngine On
FallbackResource /index.php